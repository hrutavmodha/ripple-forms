import { MyContext } from './TestContext.ripple';

export default component TestChild() {
    const value = MyContext.get();

    <div>
        <h2>{"Test Child"}</h2>
        <p>{"Value from context: " + value}</p>
    </div>
}